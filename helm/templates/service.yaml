apiVersion: v1
kind: Service
metadata:
  name: {{ include "hpc-interface.fullname" . }}
  labels:
    {{- include "hpc-interface.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hpc-interface.selectorLabels" . | nindent 4 }}

